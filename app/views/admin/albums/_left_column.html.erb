<% content_for :left_column do %>
	<h3><%= link_to 'All albums', admin_albums_url %></h3>
	<% temp = Album.year_range
	unless temp.blank? %>
		<ul class="nav">
			<% temp.each_with_index{|year, index| %>
				<li <%= "class='last'" if temp.size-1 == index %>><%= link_to year, admin_albums_url(:year => year) %></li>
			<% } %>
		</ul>
	<% end %>
<% end %>